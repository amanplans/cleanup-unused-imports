import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
    selector: 'app-unused',
    template: `An unused component`,
})
export class UnusedComponent {
}
